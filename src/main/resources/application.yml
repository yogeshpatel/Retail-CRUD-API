spring:
  application:
    name: retail-crud-no-docker-no-db

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
