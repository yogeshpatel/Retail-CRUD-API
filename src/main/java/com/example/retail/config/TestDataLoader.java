package com.example.retail.config;

/**
 * Previously loaded seed data from CSV files into in-memory maps.
 * This class is no longer active — seed data is now loaded via
 * src/main/resources/data.sql which H2 runs automatically after
 * Hibernate creates the schema on startup.
 *
 * Kept as a placeholder; safe to delete.
 */
public class TestDataLoader {
    // No longer used — see data.sql
}
